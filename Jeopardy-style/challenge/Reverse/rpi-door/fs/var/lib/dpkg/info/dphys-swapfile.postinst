#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/dphys-swapfile" ]; then
	update-rc.d dphys-swapfile defaults >/dev/null
	invoke-rc.d dphys-swapfile start || exit $?
fi
# End automatically added section
